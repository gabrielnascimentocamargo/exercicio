# exercicio
trabalho de escola

#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int i;
    printf ("numeros pares\n");
    for (i=0; i<11; i++)
    {
        if (i%2==0)
        {
            printf ("\t-> %d\n",i);
        }
    }
    return 0;
}
