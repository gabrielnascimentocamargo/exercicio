# exercicio
trabalho de escola (Nava)

#include <stdlib.h> 
#include <stdio.h> 
#include <string.h>

int main (void) 
{ 

  int i, a;
  char seunome[30]; 

  printf ("Digite um nome:\n");
  gets (seunome);

  a = strlen (seunome);
  
  for (i=a; i>=0; i--)
  {
    printf ("%s\n",&seunome[i]);
  }
return 0; 
}
