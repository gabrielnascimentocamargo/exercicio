# exercicio
trabalho de escola (Nava)

#include <stdlib.h>
#include <stdio.h>

int main (void) 
{ 
  system ("color 0b"); 
  int i, a;

  while (i >= 1) 
  { 
    printf ("Digite um valor:\n "); 
    scanf ("%d",&i);

    if (i>=1)
    {

      for (a = 1; i > 1; i = i - 1)
      {
        a = a * i;
      }

    printf ("seu fatorial é %d\n",a);
    } 
  } 
return 0; 
}
